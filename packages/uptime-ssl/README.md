# uptime-ssl

Monitora disponibilidade, status HTTP, latência e expiração TLS. Execute com um
JSON contendo `targets`, `intervalSeconds` e, opcionalmente, `webhook`. Use
`--once` para integração com cron; sem essa opção o processo agenda as checagens.
